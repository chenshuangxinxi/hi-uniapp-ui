/**
 * 组件属性
 *
 * @author 济南晨霜信息技术有限公司
 * @mobile 18560000860 / 18754137913
 */
export default {
    // 高度
    height: {
        type: String,
        default: ""
    },

    // 背景
    bg: {
        type: String,
        default: ""
    },

    // 圆角
    radius: {
        type: String,
        default: ""
    },

    // 图标名称
    iconName: {
        type: String,
        default: "__kongshuju"
    },

    // 图标颜色
    iconColor: {
        type: String,
        default: ""
    },

    // 图标大小
    iconSize: {
        type: String,
        default: ""
    },

    // 图标裁剪模式
    iconMode: {
        type: String,
        default: ""
    },

    // 提示文字
    tips: {
        type: String,
        default: "Ops! 暂无数据~"
    },

    // 提示文字大小
    tipsSize: {
        type: String,
        default: "14"
    },

    // 提示文字颜色
    tipsColor: {
        type: String,
        default: ""
    },

    // 是否显示按钮
    showBtn: {
        type: Boolean,
        default: false
    },

    // 按钮主题
    btnTheme: {
        type: String,
        default: "primary"
    },

    // 按钮文字
    btnText: {
        type: String,
        default: "继续逛逛"
    }
};
